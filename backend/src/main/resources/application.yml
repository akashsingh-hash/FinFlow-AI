server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: finflow-ai
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-ancient-block-ai6hi4nt-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require}
    username: ${SPRING_DATASOURCE_USERNAME:neondb_owner}
    password: ${SPRING_DATASOURCE_PASSWORD:npg_pJU3KTzN2Fhu}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

finflow:
  jwt:
    # 256-bit secret key in base64 or plain string
    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration-ms: 86400000 # 24 hours
    refresh-expiration-ms: 604800000 # 7 days
  storage:
    local-dir: ./uploads
